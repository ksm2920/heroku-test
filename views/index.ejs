<!DOCTYPE html>
<html lang="en">
    <%- include('head.ejs') %>  
    <body>
        <div class="todo-box">
            <h2>To-Do List</h2>
            <div class="todo-content">
                <form action="/" method="post" class="todo-input">
                    <input type="text" name="task">
                    <button type="submit"><span class="fas fa-plus"></span></button>
                </form>
                <p class="error"><%=error%></p>
                <p class="success"><%=success%></p>
                <div class="sort">
                    <a href="/?page=<%=page%>&sorted=-1">Newest</a>
                    <a href="/?page=<%=page%>&sorted=1">Oldest</a>
                </div>
                <div class="list">
                    <%- include('todo-item.ejs') %>  
                </div>
            </div>

            <div class="show">
                <% if(maxPageNr >= page) { %>
                    <a href="/?page=<%=page+1%>&sorted=<%=sorted%>">SHOW MORE</a>
                <% } else if (maxPageNr > 0 && maxPageNr < page) { %>
                    <a href="/?page=1&sorted=<%=sorted%>">SHOW LESS</a>
                <% } else { %>
                    <p></p>
                <% } %>
            </div>
        </div>
    </body>
</html>


